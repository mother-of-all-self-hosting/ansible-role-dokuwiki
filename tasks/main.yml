# SPDX-FileCopyrightText: 2023 Slavi Pantaleev
# SPDX-FileCopyrightText: 2025 shukon
# SPDX-FileCopyrightText: 2025, 2026 Suguru Hirahara
#
# SPDX-License-Identifier: AGPL-3.0-or-later

---
- name: Perform DokuWiki installation tasks
  when: dokuwiki_enabled | bool
  tags:
    - setup-all
    - setup-dokuwiki
    - install-all
    - install-dokuwiki
  block:
    - name: Validate DokuWiki configuration
      ansible.builtin.include_tasks: "{{ role_path }}/tasks/validate_config.yml"
    - name: Install DokuWiki
      ansible.builtin.include_tasks: "{{ role_path }}/tasks/install.yml"

- name: Perform DokuWiki uninstallation tasks
  when: not dokuwiki_enabled | bool
  tags:
    - setup-all
    - setup-dokuwiki
  block:
    - name: Uninstall DokuWiki
      ansible.builtin.include_tasks: "{{ role_path }}/tasks/uninstall.yml"
